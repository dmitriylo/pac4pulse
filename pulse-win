function FindProxyForURL(url, host) {
    if (
        host == "telegram.org" ||
        shExpMatch(host, "*.telegram.org")
    ) {
        return "PROXY 103.200.28.242:20915";
    }

    return "DIRECT";
}
